.venv/
out/
vendor/*.jar
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
.env
.tmp/

# Internal release governance: manifests, approvals and reports.
# Not part of the published artifact — it describes the artifact.
.munshi/

# Build products: the frozen release tarball is derived from a commit, so it
# must not be inside the tree whose hash it certifies.
dist/
